import Title from 'components/Title';
import React, { FC } from 'react';

const Schedule: FC = () => {
  return (
    <div>
      <Title text="Horario?" />
    </div>
  );
};

export default Schedule;
